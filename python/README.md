# advent-of-learning - Python repo

create env (dot file for organization): 
``` python3 -m venv .env ```

open env:
``` source .env/bin/activate ```

close env:
``` deactivate ```

Save requirements: 
``` pip freeze > requirements.txt ```

Gen env, run inside env:
``` pip install -r requirements ```